// Copyright © 2026 BxKangKi. Licensed under the MIT License.

/**
 * @file WorldSelectionWidget.h
 * 역할: 월드 목록을 구성하고 선택된 월드를 여는 UI를 관리합니다.
 * 공통 생성 버튼 스타일·레이아웃·수명 관리는 USelectionWidgetBase가 담당합니다.
 */

#pragma once

#include "CoreMinimal.h"
#include "UI/StartWorldWidget.h"
#include "WorldSelectionWidget.generated.h"

class UPanelWidget;

/**
 * World-selection menu. UStartWorldWidget provides world-opening behavior and inherits the common
 * generated selection-entry implementation from USelectionWidgetBase.
 */
UCLASS(Blueprintable, BlueprintType)
class GLTFSIMULATOR_API UWorldSelectionWidget : public UStartWorldWidget
{
    GENERATED_BODY()

public:
    virtual void NativeConstruct() override;
    virtual void SetWorldSelectionData(const TMap<FString, FString>& Values) override;

    /** Assigns the panel that receives generated world buttons and rebuilds the list. */
    UFUNCTION(BlueprintCallable, Category="Start World|World Selection")
    void SetWorldListPanel(UPanelWidget* InWorldListPanel);

    /** Rebuilds the generated button list from GetFolderNameMap(). */
    UFUNCTION(BlueprintCallable, Category="Start World|World Selection")
    void RebuildWorldButtons();

    /** Removes only entries generated by this native widget. */
    UFUNCTION(BlueprintCallable, Category="Start World|World Selection")
    void ClearWorldButtons();

protected:
    virtual void OnSelectionEntryActivated(const FString& SelectionKey) override;
};
